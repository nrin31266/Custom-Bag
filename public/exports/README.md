Runtime browser code cannot write files into this folder in a static Next.js app.
Keep this directory for future API/server-side export output, while the current
prototype stores canvas JSON and PNG data URLs in localStorage.
